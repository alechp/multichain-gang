# Asset provenance

The first release uses no raster image, Robinhood logo, or synthetic brand
mark. All diagrams are native HTML/SVG shapes in `index.html`. The local Fuse
build is reused from `../vendor/` under its existing Apache-2.0 license.
